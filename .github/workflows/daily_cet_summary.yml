name: Daily CET Trading Summary to Discord

on:
  schedule:
    - cron: '5 21 * * 1-5'   # Runs 22:00 CET / 21:00 UTC all year
  workflow_dispatch: {}

jobs:
  daily-summary:
    runs-on: ubuntu-latest
    env:
      TZ: Europe/Prague

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Get today's date (CET/CEST)
        id: dates
        run: |
          TODAY=$(date +"%Y-%m-%d")
          PRETTY=$(date +"%b %d, %Y")
          echo "today=$TODAY" >> $GITHUB_OUTPUT
          echo "pretty=$PRETTY" >> $GITHUB_OUTPUT

      - name: Fetch all trades from Base44
        id: fetch
        run: |
          mkdir -p tmp

          # FIX 1 — Use correct Base44 header
          curl -s -H "api_key: ${{ secrets.BASE44_API_KEY }}" \
            "https://app.base44.com/api/apps/6921d8e3ab33f86e081375a1/entities/Trade?limit=1000" \
            -o tmp/trades.json

          if ! jq -e 'type=="array" or (type=="object" and has("items"))' tmp/trades.json > /dev/null; then
            echo "❌ Invalid Base44 response:"
            cat tmp/trades.json
            exit 1
          fi

      - name: Build daily metrics & lists
        id: build
        run: |
          TODAY="${{ steps.dates.outputs.today }}"

          USER_EMAIL="retirehustler@gmail.com"

          # Normalize trade list (array or items[])
          if jq -e 'type=="array"' tmp/trades.json >/dev/null; then
            ITEMS=$(cat tmp/trades.json)
          else
            ITEMS=$(jq -c '.items // []' tmp/trades.json)
          fi

          # Filter ONLY your trades
          ITEMS=$(echo "$ITEMS" | jq 'map(select(.created_by == "'"$USER_EMAIL"'"))')

          ########################################
          # CLOSED TODAY
          ########################################
          CLOSED_JSON=$(echo "$ITEMS" | jq --arg d "$TODAY" '
            map(select((.close_date|tostring) | startswith($d)))
          ')

          ########################################
          # OPEN TODAY
          ########################################
          OPEN_JSON=$(echo "$ITEMS" | jq --arg d "$TODAY" '
            map(select((.open_date|tostring) | startswith($d)))
          ')

          ########################################
          # FORMAT TWEET-LIKE LINES
          ########################################
          format_closed() {
            echo "$CLOSED_JSON" | jq -r '
              .[] |
              # prefix rules
              (.direction as $dir | .status as $st |
                if $dir=="BUY" and $st=="Closed" then "Sell to Close"
                elif $dir=="SELL" and $st=="Closed" then "Buy to Close"
                else "Buy to Close"
                end
              ) as $prefix |

              # clean numbers (strip .0)
              (.quantity|tostring | sub("\\.0$";"")) as $qty |
              (.strike_price|tostring | sub("\\.0$";"")) as $s1 |
              (.strike_price_2|tostring | sub("\\.0$";"")) as $s2 |

              # prices
              (.close_price // .open_price // 0) as $px |

              # profit
              (.profit_loss // 0) as $p |

              # ROI 2 decimals
              ((.roi // 0) * 100 | tostring | capture("(?<x>^-?\\d+(?:\\.\\d{0,2})?)") | .x) as $roi |

              "\($prefix) \($qty) \(.ticker) - \(.type) - \($s1)/\($s2) @ \($px) for $\($p) profit with \($roi)% ROI 🔥"
            '
          }

          format_open() {
            echo "$OPEN_JSON" | jq -r '
              .[] |
              (.direction as $dir | .status as $st |
                if $dir=="BUY" and $st=="Open" then "Buy to Open"
                elif $dir=="SELL" and $st=="Open" then "Sell to Open"
                else "Sell to Open"
                end
              ) as $prefix |

              (.quantity|tostring | sub("\\.0$";"")) as $qty |
              (.strike_price|tostring | sub("\\.0$";"")) as $s1 |
              (.strike_price_2|tostring | sub("\\.0$";"")) as $s2 |

              "\($prefix) \($qty) \(.ticker) - \(.type) - \($s1)/\($s2) @\(.open_price) Exp \(.expiration_date)"
            '
          }

          CLOSED_LINES=$(format_closed)
          OPEN_LINES=$(format_open)

          ########################################
          # Daily P&L
          ########################################
          DAILY_PL=$(echo "$CLOSED_JSON" | jq '[.[].profit_loss // 0] | add // 0')

          # Fallbacks
          if [ -z "$CLOSED_LINES" ]; then CLOSED_LINES="(no closed trades today)"; fi
          if [ -z "$OPEN_LINES" ]; then OPEN_LINES="(no opened trades today)"; fi

          CLOSED_ESCAPED=$(printf "%s" "$CLOSED_LINES" | python3 - << 'PY'
          import json,sys
          print(json.dumps(sys.stdin.read()))
          PY
          )
          OPEN_ESCAPED=$(printf "%s" "$OPEN_LINES" | python3 - << 'PY'
          import json,sys
          print(json.dumps(sys.stdin.read()))
          PY
          )

          echo "daily_pl=$DAILY_PL" >> $GITHUB_OUTPUT
          echo "closed_list=$CLOSED_ESCAPED" >> $GITHUB_OUTPUT
          echo "open_list=$OPEN_ESCAPED" >> $GITHUB_OUTPUT

      - name: Send Discord message
        env:
          DISCORD_WEBHOOK: ${{ secrets.DAILY_DISCORD_WEBHOOK_URL }}
        run: |
          PRETTY='${{ steps.dates.outputs.pretty }}'
          PL='${{ steps.build.outputs.daily_pl }}'
          CLOSED='${{ steps.build.outputs.closed_list }}'
          OPEN='${{ steps.build.outputs.open_list }}'

          PAYLOAD=$(cat <<EOF
          {
            "embeds": [{
              "title": "📊 Daily Trading Summary – $PRETTY ",
              "color": 3447003,
              "fields": [
                { "name": "💰 Daily Closed P&L", "value": "\`$PL\`", "inline": true },
                { "name": "✅ Closed Today", "value": $CLOSED },
                { "name": "📂 Opened Today", "value": $OPEN }
              ],
              "timestamp": "$(date -u +%Y-%m-%dT%H:%M:%S.000Z)",
              "footer": { "text": "RetireHustler Options Edge" }
            }]
          }
          EOF
          )

          curl -s -H "Content-Type: application/json" -d "$PAYLOAD" "$DISCORD_WEBHOOK" -o /dev/null -w "%{http_code}\n"
